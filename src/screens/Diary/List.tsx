import React from 'react';
import { View, Text } from 'react-native';

const List = (): React.ReactElement => {
  const text = 'List';
  return (
    <View>
      <Text>{text}</Text>
    </View>
  );
};

export default List;
