import styled from 'styled-components/native';

export const DateTimePickerWrapper = styled.View`
  flex: 1;
  position: absolute;
  left: 0;
  right: 0;
  bottom: 0;
  background-color: black;
`;

export const IOSHeaderButtonWrap = styled.View`
  flex-direction: row;
  justify-content: space-between;
  align-items: center;
  padding: 5px 30px;
`;
